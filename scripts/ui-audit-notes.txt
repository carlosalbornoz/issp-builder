"""
UI Issues found via code audit:
1. `<button>` elements missing `type="button"` → can accidentally submit parent forms
   Files: part2-a-form.tsx:155, part3-c-form.tsx:134, part1-c-form.tsx:226
2. `size="icon"` Buttons missing `aria-label` → inaccessible to screen readers (12 instances)
3. `console.log` left in src/proxy.ts
4. `IsspEditorShell` - `creatorName` hardcoded as "" instead of coming from layout
5. Textareas with `rows` set but NO `resize-none` class (3 instances in part2-a and part2-c)
   → users can't resize, but the cursor shows resize, confusing UX → should add resize-none OR allow resize
6. The main content padding `p-8` is too much on mobile — should use responsive padding
7. `max-w-4xl` on content but the sidebar is 18rem (w-72) → on medium screens the content area can feel cramped
"""
